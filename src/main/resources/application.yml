spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/senoMentoriaDB
    username: seno
    password: seno
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always